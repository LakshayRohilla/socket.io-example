// configureStore with:
// reducer: register api.reducer under api.reducerPath.
// middleware: concat api.middleware for RTK Query behavior. How it connects:
// Provider in main.jsx makes store available to hooks like useDispatch/useSelector and RTK Query hooks.